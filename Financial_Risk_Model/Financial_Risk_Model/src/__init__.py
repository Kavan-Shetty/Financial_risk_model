"""
Financial Risk Model — src package
===================================
Public API of the source package.

Import the key pipeline components directly:
    from src import data_loader, feature_engineering, preprocessing,
                   model_training, model_evaluation, prediction_pipeline
"""
